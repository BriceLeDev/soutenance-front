/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseAbonnementResponse } from '../../models/page-response-abonnement-response';

export interface GetAbonnementByOwner$Params {
  'user-id': number;
  page?: number;
  size?: number;
}

export function getAbonnementByOwner(http: HttpClient, rootUrl: string, params: GetAbonnementByOwner$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseAbonnementResponse>> {
  const rb = new RequestBuilder(rootUrl, getAbonnementByOwner.PATH, 'get');
  if (params) {
    rb.path('user-id', params['user-id'], {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseAbonnementResponse>;
    })
  );
}

getAbonnementByOwner.PATH = '/abonnement/by-user/{user-id}';
